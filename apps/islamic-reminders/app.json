{
  "name": "islamic-reminders",
  "description": "Islamic Reminders – daily hadith aligned with the Hijri calendar",
  "repository": "https://github.com/selghribi-blip/selghribi-blip.github.io",
  "keywords": ["islamic", "hadith", "reminders", "hijri"],
  "env": {
    "MONGODB_URI": {
      "description": "MongoDB connection string (Atlas recommended for Heroku)",
      "required": true
    },
    "JWT_SECRET": {
      "description": "Secret key for signing JWT tokens. Use a long random string.",
      "generator": "secret",
      "required": true
    },
    "JWT_EXPIRES_IN": {
      "description": "JWT expiry duration (e.g. 7d, 30d)",
      "value": "7d"
    },
    "NODE_ENV": {
      "description": "Node environment",
      "value": "production"
    },
    "ALLOWED_ORIGINS": {
      "description": "Comma-separated list of allowed CORS origins",
      "required": false
    }
  },
  "formation": {
    "web": {
      "quantity": 1,
      "size": "basic"
    }
  },
  "buildpacks": [
    { "url": "heroku/nodejs" }
  ]
}
